[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send all routes to index.html so /penthouses/:slug works on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
